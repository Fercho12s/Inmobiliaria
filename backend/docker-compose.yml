services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - db_data:/app/data
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - DB_PATH=/app/data/vendrixa.db
    env_file:
      - .env

volumes:
  db_data:
